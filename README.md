# rhyme-tensorflow-flask
Guidede project to achieve two main goals:

  * Serve a TensorFlow model with TensorFlow Serving and Docker.
  * Create a web application with Flask to work as an interface to a served model.
